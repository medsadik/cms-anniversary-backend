
spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://keycloak.cmscompany.net/realms/cms
  datasource:
    url: ${JDBC_DATABASE_URL:jdbc:postgresql://localhost:5432/anniversaryDB}
    username: ${JDBC_DATABASE_USERNAME:postgres}
    password: ${JDBC_DATABASE_PASSWORD:Cms@2025}
    hikari:
      connection-timeout: 30000
      maximum-pool-size: 10
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
        id:
          new_generator_mappings: true
  mail:
    host: smtp.office365.com
    port: 587
    username: supportit@cmscompany.net
    password: M@rina2424
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
error:
  handling:
    http-status-in-json-response: true
    exception-logging: NO_LOGGING
    full-stacktrace-http-statuses: 5xx
    log-levels:
      5xx: ERROR
springdoc:
  pathsToMatch: /
hr:
  api:
    base-url: https://rhna.cmscompany.net
    employees-endpoint: /APIQualiso/employes
    connection-timeout: 5000
    read-timeout: 10000
scheduling:
  sync:
    cron: "0 0 10 * * *"
  email:
    cron: "0 9 12 * * *"

email:
  from: supportit@cmscompany.net
  from-name: Company HR Team
server:
  port: 8080
